# churl — Architecture

## Crate layout

### `churl-core` (library)

Zero TUI dependencies — ever. This constraint is enforced by code review and CI; adding `ratatui`/`crossterm`/`termion` to this crate is always wrong.

| Module | Responsibility |
|---|---|
| `model` | Core types: `Method`, `Endpoint`, `Request`, `Response`, `Header`, `Param`, `Auth`/`ApiKeyPlacement` (M5: internally-tagged `[request.auth]`) |
| `assert` | Response assertion model + evaluator (M8.4, UI-free): `Assertion { target, op, value }` — `target` reuses `sequence::extract_value` verbatim (same extraction grammar); `AssertOp` (`Eq`/`Ne`/`Contains`/`Exists`/`Absent`/`Lt`/`Gt`/`Le`/`Ge`) serializes as its canonical string (`==`, `contains`, …), never the variant name. `Assertion::parse` reads the human `"<target> <op> <value>"` syntax (`docs/CLI.md`). `evaluate`/`run_assertions` produce a serializable `AssertionReport` the CLI drops straight into the JSON envelope's `data.assertions` |
| `auth` | `apply_auth(&Auth) -> AuthWire` — THE single dispatch point on auth kinds (M9 plugin guardrail); resolves basic/bearer/apikey to a `Header` or `Query` wire effect that `execute`/`export` apply without ever matching on `Auth` |
| `persistence` | TOML round-trip via `toml_edit` (format-preserving, deletion-pruning `merge_tables`; arrays-of-tables merge **element-wise regardless of length** via `merge_arrays_of_tables` — survivors keep their decor on grow/shrink, R1 D3); lazy collection loading. `Collection::endpoints()` is strict; `endpoints_lenient() -> CollectionLoad { endpoints, warnings }` degrades one unparseable file to a warning (TUI load path). Both skip `folder.toml` **and** `churl.toml` (a nested-workspace manifest is not an endpoint). Sequences (M7.4): `SEQUENCES_DIRNAME` reserved dir (excluded from `collections()`); `OpenWorkspace::sequences() -> SequenceLoad { sequences, warnings }` (lenient); `load/save/create/rename/delete_sequence` CRUD seams. **Reserved-name guards (R1 D1)**: create/rename disambiguate a slug colliding with a reserved *file* stem (`RESERVED_FILE_STEMS = churl/folder`, via `unique_endpoint_path`) or reserved *dir* name (`RESERVED_DIR_NAMES = sequences`, via `collection_dir_name`) — `churl` → `churl-2.toml`, never overwriting the manifest/folder/sequences; display `name` preserved, final stem surfaced by the caller (`slug_of` helper). A directory module (`persistence/`, M7.11 PR 6): **`mod.rs`** holds the CRUD/load/save seams + `OpenWorkspace`/`Collection`; child **`persistence/merge`** the format-preserving TOML merge cluster (`merge_tables` + recursive `merge_items`/`merge_arrays_of_tables`/`values_equal`); child **`persistence/naming`** the slug/reserved-name derivation (`slugify`, `slug_of`, `RESERVED_*`, `is_reserved_*`, `unique_endpoint_path`, `collection_dir_name`) — both re-exported so the public surface and all `use` paths are unchanged |
| `sequence` | Request-sequence run engine (M7.4, UI-free): dependency-free extraction subset (`extract_value`: `status` / `header:<Name>` / JSON-path `$.a.b[0].c`) over `serde_json`; run primitives shared by tests and the live TUI — `prepare_step` (resolver with the `extracted` scope prepended highest, then `session` — note #6), `extract_step`, `classify_step` (the single classify+extract seam), `ordered_steps`; `run_sequence` wiremock-tested convenience. `RunScopes` carries the in-memory `session` map (note #6). Rejects `..`/absolute step endpoints; never panics |
| `load` | Concurrent-load / throttle runner (M7.5, UI-free): `run_load` fires N copies of an already-resolved `Request` through the single `execute` chokepoint, bounded by `futures`' `buffer_unordered` (== concurrency in flight) + absolute-target pacing; `classify` (`Ok`/`Failed`≥400/`Error`) is the single seam; pure `stats` (nearest-rank min/median/p95/max/mean over completed timings); `check_config`/`LoadCaps`/`LoadCheck` guardrail classifier. `run_load` is the wiremock-tested twin the TUI launcher mirrors. Uses `tokio::time`/`futures` internally; `execute` stays runtime-agnostic |
| `template` | Hand-rolled `{{var}}` substitution via a single chain resolver; precedence: **in-memory Session captures (note #6)** → CLI flag → active profile → the endpoint's **collection ancestor chain** leaf→root (each `folder.toml`, ending at the root collection's `churl.toml [vars]`) → process env (the inherit-and-override collection ancestor walk replaced the earlier single collection+workspace scopes). The Session scope (note #6) is prepended for BOTH standalone sends (`App::build_resolver`) and sequence runs; sequences additionally prepend the ephemeral highest-precedence `extracted` scope (M7.4). Full run chain: `extracted > session > cli > profile > [leaf-collection … root-collection] > env` — extra scopes, resolution never forked. **Fail-loud seam (note #4b):** `unresolved_placeholders(req) -> Vec<String>` reuses the same `parse_placeholder` scan to name any well-formed `{{var}}` still present *after* substitution (sorted, deduped); the three send paths refuse to send and surface the names. There is **no** escape for literal double-braces — a malformed/literal brace run (`{{ }}`, `{{a b}}`, unclosed `{{`) is left verbatim by substitution and never flagged. Substitution itself still leaves an unresolved placeholder verbatim (single seam, M9-forward) — detection is a separate post-substitution gate, so the success path is unchanged |
| `import` | curl command parsing (shlex + hand-rolled flag map, M4): strict flag policy — unknown flags are hard errors, `-F`/`@file` are `Unsupported`, query stays in the URL; returns `ImportResult { endpoint, warnings }` |
| `export` | curl command generation from `Endpoint` (M4): shlex-quoted single line, enabled headers/params only; round-trip contract with `import` |
| `http` | Request execution via `reqwest` + `rustls`; coarse timing (`total` only, `connect` stays `None`); `execute(client, request, &ExecuteOptions)` is a plain runtime-agnostic `async fn` — cancellation is task-level in the TUI (`tokio::spawn` + `AbortHandle`), never in core. Body streamed chunk-wise up to `max_body_bytes` (default 10 MB) → `Response.truncated`; `build_client(timeout)` takes the config-resolved timeout and disables reqwest's own redirect handling (`Policy::none`); it delegates to **`build_client_with(&ClientConfig)`** (M8) — the single seam carrying the session request controls `{ timeout, proxy, insecure, cookies }`. `proxy: Some` → `Proxy::all` (unset ⇒ reqwest honors the env proxy); `insecure: true` → `danger_accept_invalid_certs(true)` (rustls: also accepts hostname mismatch, so no separate hostname knob); `cookies: Some(Arc<ChurlCookieJar>)` → `cookie_provider`. Every live proxy/TLS/cookies change rebuilds this single client (`App::rebuild_client`) — there is never more than one, and it always reflects current session state. Redirects are followed **manually** in `execute` (`follow_redirects`, hop cap 10) per `ExecuteOptions.redirect` (`RedirectPolicy::{Strict,Strip,FollowAll}`, default `Strip`): auth-bearing headers (`Authorization`/`Cookie`/secret-named/injected-auth) are dropped when a hop crosses the scheme+host+port origin, so churl — not reqwest — controls exactly which headers survive. Auth injected via `auth::apply_auth` (M5): header effects skipped when an enabled user header with the same name exists (the user's header wins), query effects appended after enabled params. No `{{var}}` templating (M6); URL/headers/body/auth used verbatim |
| `history` | SQLite via `rusqlite` (bundled); schema managed via migrations at startup. **R1 D2 durability/concurrency**: `open` sets `busy_timeout=5000` (`BUSY_TIMEOUT_MS`) + `journal_mode=WAL` (fails loud `WalUnavailable` on FS fallback; asserted for file-backed DBs only), and `migrate()` runs the whole loop inside ONE `BEGIN IMMEDIATE` transaction (re-reads `user_version` under the write lock, applies still-pending migrations, commits once — all-or-nothing; a concurrent opener blocks then no-ops). Migration 3 (M7.5) adds a SEPARATE `load_batches` table (`LoadBatchSummary` / `insert_load_batch` / `recent_load_batches`): a load run writes exactly ONE summary row there, never to `history`, so the per-endpoint history view is never flooded; migration 4 appends the `mean_ms` column via `ALTER TABLE` (migrations are append-only — never edit an applied one); migration 5 (M8) adds a `cookies` table (`workspace TEXT PRIMARY KEY, jar_json, updated_at`) storing one persistent cookie-jar blob per canonicalized workspace root (`cookie_jar`/`save_cookie_jar`), loaded on workspace open and saved after mutating sends / toggle-off / clear / exit. **M8.1 off-thread writer**: the cookie-jar write is offloaded to `history::CookieJarWriter` — a dedicated OS thread owning its OWN `HistoryStore` connection to the same file (a harmless second connection: WAL is already set, the migration runner re-reads the version and no-ops), fed coalesced `(workspace, jar_json, updated_at)` snapshots over a `Mutex<HashMap>` + `Condvar` (mirrors the `highlight` off-thread worker pattern). The UI thread serializes the jar (cheap, in-RAM) then hands off the snapshot, so the blocking `conn.execute` — which can wait up to the WAL `busy_timeout` under cross-process contention — never stalls it. Invariants: **final-state-wins** (the queue is a `workspace → latest snapshot` map and the single writer thread drains in order, so no older snapshot lands after a newer one; bounded by one row per key), **no lost writes on quit** (`shutdown` drains the queue then joins the thread — the run loop calls it after the final `persist_cookie_jar`), and **no clobber on failure** (serialization skips the enqueue on error so the writer only ever receives a good blob, and `save_cookie_jar`'s atomic upsert leaves the prior blob intact on a DB error, surfaced via `take_error`). History writes stay synchronous (out of scope). **R1 D4b pruning**: `insert`/`insert_load_batch` prune-on-insert to the newest `HISTORY_ROW_CAP`=10 000 rows/table (indexed `DELETE … WHERE id <= max_id - CAP`); `recent()` reads unaffected |
| `config` | `~/.config/churl/config.toml` (incl. flat `[keys]` override strings, `timeout_secs`, `max_body_bytes` — resolved via `Config::timeout()`/`Config::max_body_bytes()`; the M7.5 `[load]` guardrail caps via `Config::load_caps()`; the M8 `proxy`/`insecure`/`cookies` launch defaults via `Config::{proxy,insecure,cookies}()`) and per-workspace `churl.toml` (also `proxy`/`cookies`); never contains secrets. Secrets heuristics: `looks_like_secret_name`, `is_template_placeholder`, `secret_violations` (manifest), `auth_secret_violations` (endpoint auth, M5), `proxy_has_credentials` (M8 — a persisted proxy must be credential-free; the save path refuses `user:pass@`, never strips) |
| `cookies` | (M8) `ChurlCookieJar` — a thin `RwLock<cookie_store::CookieStore>` implementing reqwest's `cookie::CookieStore` trait (mirrors reqwest's own `Jar`, but over an **owned** store we can serialize). Cross-origin safety is a store property: `cookies()` does RFC 6265 domain/path/`Secure` matching, so a cookie set by host A is never sent to host B — this layers *under* the manual cross-origin `Cookie`-header strip in `http`. `to_json`/`load_json` persist only persistent+unexpired cookies; `list`/`delete`/`clear` drive the Options overlay + `churl cookies` CLI. **M8.1 poison recovery**: every guard acquisition goes through `store_read`/`store_write`, which `unwrap_or_else(|p| p.into_inner())` instead of `.expect(...)` — a panic elsewhere while holding the write guard poisons the lock, and recovery keeps every later cookie op working rather than turning one panic into a permanent crash-on-every-jar-access wedge. **M8.5.1**: `CookieView` also carries `path`/`secure`/`same_site` (a local `SameSite` enum — see `docs/DECISIONS.md`), and `upsert(CookieSpec)` adds/replaces a hand-typed cookie (always persistent, inserted via `insert_raw` against a synthesized `https://{domain}{path}` URL) for the Settings panel's cookie add/edit form |

### `churl` (binary + thin lib)

The `lib.rs` target exists solely to let integration tests (`tests/`) import internal modules (primarily `tui`). The binary (`main.rs`) is thin: parse CLI, dispatch to subcommand or TUI.

| Module | Responsibility |
|---|---|
| `main` | `Cli` (clap derive); global `--var key=value` (repeatable) + `--profile` args; `Command` variants (`import` since M4; `keymaps` since M6: prints the effective keymap sorted by action name, `(default \| overridden)`); `#[tokio::main]`; color-eyre hook installation |
| `tui` | Terminal init/restore; `run(cli_vars, profile)` entry point (config → keymap → theme → workspace → `App::with_config`; unknown profile / bad theme error before the alternate screen) |
| `tui::app` | A directory module (`app/`, M7.11) whose **`mod.rs`** is the spine — `App` state (`Pane` focus incl. `UrlBar`, `Mode` overlays incl. `Jump`/`MethodMenu`/`Prompt`/`Confirm`/`EnvEditor(EnvEditorState)` (the M7.3 env editor — R1.5 A2: its state is **owned by the `Mode` variant**, not a parallel `Option` field)/`Sequence { view, editor, runner }` (M7.4 — R1.5 A2: the unified sequence surface's edit-face/run-face/active-face state is owned by the `Mode` variant, not the old `sequence_editor`/`sequence_runner`/`sequence_view` fields; `editor`/`runner` stay `Option` inside the variant since either face may be unbuilt; `AppMsg::SequenceStep` + the still-standalone `sequence_abort` handle drive the live run across face flips)/`LoadRunner(LoadRunnerState)` (M7.5 — R1.5 A2: state owned by the variant; plus `load_request`/`load_caps`; `AppMsg::LoadStarted`/`LoadResult` + the single `load_abort` launcher drive the live batch). **R1.5 A2 (state-model hardening):** the modal overlays above carry their owned state **inside** their `Mode` variant so `(Mode::X, None)` is unrepresentable — the old parallel `Option<…State>` fields, their `is_none()→Normal` defensive guards, and the `.expect("checked above")` panics are gone. Consequently `Mode` is **non-`Copy`** and does not derive `PartialEq` (the payload states aren't cheaply comparable), so variant checks use `matches!` and values are moved with `mem::replace` — notably `body_search_return: Mode` now *parks* the whole `LoadRunner(state)` / `Sequence{..}` mode across a body-search overlay, resolved back through mode-aware accessors (`load_runner()`/`sequence_runner()` + the `*_in_mut` disjoint-borrow forms). **The open fuzzy-picker overlay is likewise a data-carrying `enum Picker` (R1.5 A2 PR 3, audit H3)** — `picker: Option<Picker>` replaces the old bare `Option<PickerState>` that was multiplexed against three parallel item `Vec`s (`profile_choices`/`workspace_choices`/`sequence_choices`) + `search_targets`/`palette_actions` and three one-shot `bool`s (`load_runner_after_pick`/`sequence_pick_runs`/`auth_picker`). Each variant (`Search{targets, after_pick}`/`Palette{actions}`/`Profile{profiles}`/`Workspace{paths}`/`Sequence{paths, runs}`/`Auth`) carries its own items + one-shot intent alongside the shared `PickerState` finder, so the selected index can only address its own list (the H3 out-of-bounds hazard is gone) and a stale flag can't leak across kinds; `accept_overlay` `take`s the picker and matches the variant, reached uniformly via `picker_state()`/`picker_state_mut()`. The `Mode::Search`/`Palette`/`WorkspacePicker`/`SequencePicker` variants stay as the routing/render axis (not folded into `Picker` — scope control). Remaining overlay-axis states (`help_open`/`leader`/`url_popup`) are folded in later R1.5 PRs, `RequestTabs`, `loaded_snapshot` for derived dirty, inline `url_editor`/`prompt_editor`, `AppMsg`, active profile, cli vars, `Theme`, `tick_count`); key routing via `lookup_ctx(key, pane.ctx())`; inline `LineEditor` editing (URL bar + request rows + CRUD prompts); in-app CRUD via `churl-core::persistence` seams; send-time `{{var}}` resolution; `tokio::select!` loop. Child modules keep full private-field access to `App` without any `pub(crate)` widening (see DECISIONS.md, "Module boundaries"): **`app/render`** — the top-level `render` entry point (re-exported as `app::render`) + its draw-only helpers (`leader_popup`, `render_collapsed_stub`, `prompt_hint`, `confirm_text`) painting the two-column layout: Explorer (left) + three-row column B: focusable URL bar / Request (tabs) / Response; **`app/pure`** — the `self`-free helpers (URL `split_query`/`percent_decode`/`merge_query_params`, Auth-tab `auth_field_*`/`toggle_auth_placement`, export-path `default_export_path`/`export_target`); **`app/state`** (M7.11 PR 6) — the pure data-type definitions (`Pane`/`LeftPane`/`Mode`/`PromptPurpose`/`ConfirmPurpose`/`AppMsg`/`LeaderState`/`SeqView`/`ZoomPane` enums, the per-endpoint `Buffer`/`EndpointBuffer` state + `InFlightRequest`/`PendingCopy`/`PendingLoad`/`PendingClose`, the `fold_body_text`/`overwrite_body_text` body-folders) + their trivial impls, re-exported so `App`'s public type paths are unchanged. The **`app/handlers/`** module (M7.11 PR 3–4) holds handler clusters as grandchild `impl App` blocks (same child-module privacy — methods carry their original `pub(in crate::tui::app)` scope, no widening): `handlers/buffers` (buffer/tab open-focus-cycle-close + dirty/save-all), `handlers/help` (help-overlay key routing + in-help search), `handlers/env_editor` (env-editor overlay open/key/save/close), `handlers/load_runner` (load-runner open/start/progress/cancel/summary/key), `handlers/response` (response-viewer scroll/toggles/body-search/copy), and (PR 4, the stateful/async clusters) `handlers/send` (request dispatch/cancel via the bounded channel + spawned tasks + generation guard, the `AppMsg` pump, response landing, highlight caching, history writes), `handlers/sequence` (sequence run orchestration + the unified-surface key routing + pickers + the editor save/create path), `handlers/editing` (row/field/auth/method/url editing + save), `handlers/crud` (endpoint & collection create/rename/delete, import/export, paste-curl, copy-as-curl, prompt/confirm key routing), and (PR 6) `handlers/vars` (variable-scope + template-resolver plumbing: `profile_vars`/`workspace_vars`/`session_*`/`build_resolver`/`build_env_resolver`) and `handlers/workspace` (tree-reload/buffer-remap seam + guarded endpoint/workspace switch: `reload_explorer`/`remap_buffers`/`select_endpoint_file`/`guarded_load`/`perform_load`/`switch_workspace`). Inline `#[cfg(test)] mod tests` lives in **`app/tests.rs`** |
| `tui::events` | A directory module (`events/`, M7.11) whose **`mod.rs`** holds the keymap/event-routing core (all re-exported paths unchanged) with two sibling child modules for full private-scope reach and no widening (see DECISIONS.md, "Module boundaries"): **`events/action`** — the `Action` enum (+ `Jump`, `SwitchProfile`, and the M6.6 URL-bar/tab/row/CRUD/save actions), its private `ACTION_TABLE`/`FOCUS_BUFFER_TABLE` lookup tables (`pub(in crate::tui::events)`, consulted by the keymap default), and the `FromStr`/`Display` name↔action conversions (+ `UnknownAction`); **`events/fuzzy`** — `FuzzyFinder` (nucleo-matcher). The `mod.rs` core keeps: crokey `KeyMap` with per-pane `overlays` (`PaneCtx ∈ {Explorer, UrlBar, Request, Response}`; `lookup_ctx` = overlay-then-global) built from `[keys]` + `[keys.<pane>]` config; `leader_root` (root leader continuations, `LeaderEntry::{Act, Submenu(name)}`) + **data-driven** `submenus: HashMap<String, Submenu{title, binds}>` (built-in `sequences`/`load`/`tabs` seeded as defaults; `[keys.leader.<name>]` creates/extends any submenu — M7.10); `validate(global, overlays) -> Vec<String>` load-time conflict/shadow warnings (leader-as-action, dangling/orphan submenu, in-scope duplicate combo, globally-shadowed global); `iter`/`combos_for`/`leader_combos_for` (+ overlay variants) for the `keymaps` subcommand |
| `tui::theme` | `Theme` (named style slots) parsed from core config strings, mirroring `[keys]`: built-in `dark`/`light` + `[theme_colors]` per-slot overrides (named ANSI or `#rrggbb`); fails loudly on unknown built-in/slot/colour. Core stays TUI-free (carries strings only) |
| `tui::highlight` | Off-thread syntect worker: a dedicated `std::thread` owns the `SyntaxSet`/theme (lazy-loaded on first job), receives `HighlightJob`s over `std::sync::mpsc`, returns `AppMsg::Highlighted` over the bounded app channel via `blocking_send` (R1 D4a — safe: a non-async OS thread, never the render/UI thread, so it parks under backpressure without stalling input). Embedded theme follows the UI theme (Nord dark / InspiredGithub light). `SyntaxToken` (json/xml/html/plain) derives from the response `Content-Type`. **Best-effort spawn (R1.5 B4)**: `spawn` returns `Option<Sender<HighlightJob>>` — a thread-spawn refusal (EAGAIN under fd/thread exhaustion) yields `None` instead of panicking, so `install_runtime` leaves `highlight_tx: None` and the viewer renders plain (the same degraded path `TestBackend` already takes); every render site already guards on `if let Some(tx)` |
| `tui::components` | One module per pane/overlay: `explorer` (recursive collection-tree state machine (M7.9): a flat `Vec<CollectionNode>` with node 0 = the root collection (its endpoints + sub-collections render at depth 0; never a container row), each node carrying `parent`/`depth`/lazy `children`+`endpoints`, `collection` a stable index; viewport scroll offset, cached `folder.toml` vars + `collection_ancestor_vars` for the resolver, `cursor_collection_dir` for cursor-aware create, CRUD-support accessors + `reload`/`select_file`, lenient-load `take_warnings`), `urlbar` (focusable strip: method/URL + inline edit cursor + `●` dirty dot + auth kind + placeholder count), `line_editor` (shared single-line editor — no dep), `request` (tab bar + Params/Headers/Auth row lists + edtui Body), `request_tabs` (pure tab/row/field-edit state machine), `response` (a directory module (`response/`, M7.11): **`mod.rs`** holds the `ResponseView`/`ResponseState`/`ResponseGeometry` types + their core state impl (accessors, folds, wrap, search, copy); child **`response/render`** = the render/draw pipeline (`render` + `RenderCtx`/`RenderOutcome` + private draw helpers + `status_summary`/`collapsed_summary`/`fmt_bytes`) and child **`response/text`** = the text-processing helpers (`sanitize_for_display`, `reformat_body_if_needed`/pretty JSON, `index_lines`, `expand_wrap`, char-slice/offset mapping); both children reach `ResponseView`'s private fields with no widening. Virtualised viewer + M7 display pipeline: cursor, headers view, wrap, JSON folding, body search, copy; exports the shared `smart_case_matches` matcher + `build_search_spans` painter reused by the help overlay), `help` (`?` overlay rendered from the live keymap — sectioned, scrollable, plus the M7.7 Stage B `/` highlight-and-jump search), `fold` (string-aware JSON fold-region scanner), `env_editor` (a directory module (`env_editor/`, M7.11): **`mod.rs`** holds the `EnvEditorState` type + its core state/accessors + `save` + `map_to_rows`/`rows_to_map`, child **`env_editor/edit`** (PR 6) the key-handling impl (`handle_*_key` routers + edit-mode/profile-CRUD state transitions), child **`env_editor/render`** the render/draw fns (modal, scope list, var rows, footer); both children reach `EnvEditorState`'s private fields/methods with no widening. M7.3 environments & vars editor: split-view over workspace/collection/profile var scopes — profile CRUD, dirty/discard guard, secret mask+refuse, live precedence display; all UI state here, core stays UI-free), `picker` (generic fuzzy overlay), `method_menu` (one-key method picker), `prompt` (CRUD text prompt + y/n confirm overlays), `search`, `palette` (curated command allowlist), `jump`, `statusline`, `sequence_runner` (M7.4 live run view — reuses `response::render` + carries a `ResponseGeometry`; its Response-region keys route through the shared `response_*` handlers (note #2), UI-only), `sequence_editor` (M7.4 §4 — steps + extraction-rule CRUD + reorder, saves via `save_sequence`), `load_runner` (M7.5 concurrent-load runner: editable config header (total/concurrency/interval via the M7.3 LineEditor field-row pattern) + live O(viewport) results list + reused `response::render` viewer (carries a `ResponseGeometry`; Response-region keys route through the shared `response_*` handlers, note #2) + live stats line; UI-only — `App` owns the single `buffer_unordered` launcher + abort handle + generation guard + guardrail. **R0 memory bound**: retention is O(concurrency + K) — a `live_views` deque keeps full `ResponseState::Done` views only for the last `LIVE_VIEW_WINDOW`=16 completions + the selected row; overflow downgrades the oldest non-selected row to `ResponseState::Dropped { status, timing, size }` (no body bytes, not reconstructable), rendered as a "not retained" placeholder. Stats over all outcomes are unaffected) |
| `tui::clipboard` | Clipboard writes via OSC 52 (`ESC ] 52 ; c ; <base64> BEL`) to ratatui's terminal backend writer — no native clipboard dep (works over SSH/tmux; macOS Terminal.app ignores it). 1 MB payload cap |

### Event / render loop (M2+)

```
crossterm EventStream ──┐
tick timer (250 ms) ────┤──► tokio::select! ──► handle_key() ──► state mutation
app mpsc channel ───────┘                                     └──► terminal.draw()

HTTP requests (M3): spawned as tokio tasks, each with an AbortHandle.
Results arrive on the app mpsc channel as AppMsg::Response { .. }.
The render loop never awaits I/O.
```

**Bracketed paste.** `tui::init` requests bracketed paste (`EnableBracketedPaste`, disabled again in `restore`), so a multi-line clipboard paste (e.g. a browser "Copy as cURL") arrives as a single `Event::Paste(text)` instead of a key stream whose embedded newlines would submit a prompt early. The run loop routes it to `App::handle_paste`, whose routing **mirrors `handle_key`'s precedence exactly** — because bracketed paste is enabled globally, every surface that accepts typed keys must also accept a paste, or the payload would silently vanish. Covered: help-search, the fuzzy pickers/palette (`PickerState::push_str` + refilter), body-search, the env-editor sub-inputs (session-var / profile-name / row-field, via `EnvEditorState::paste` which live-mirrors the row), the Options proxy field, the sequence editor (add-step filter / rule field, `SequenceEditorState::paste`), the load-runner numeric field (digit-filtered, `LoadRunnerState::paste`), the inline URL editor, the request-row `FieldEdit`, and every other `Mode::Prompt` purpose (all `LineEditor::insert_str`). The edtui multi-line surfaces (Body editor, URL popup) get the **raw** payload via `on_paste_event`, gated on an insert-capable (non-Normal) mode so the text lands at the cursor. The new-endpoint prompt (`Mode::Prompt(PromptPurpose::NewEndpoint)`) is two-stage: while its `curl_prompt` is still `None` (the default light `LineEditor` sub-state), a paste that starts with `curl` or contains a newline **expands** into the multi-line edtui editor in Normal mode (seeded with any prior typed text plus the paste, newlines normalised to `\n` first since the curl parser's `\`-continuation logic keys on LF) instead of being inserted as a name; a short single-line non-curl paste falls through to the plain `LineEditor::insert_str`, same as every other purpose. Once `curl_prompt` is `Some` (already expanded), a paste behaves like the Body/URL popup case — raw payload via `on_paste_event` at the cursor, newlines normalised first. `LineEditor`/picker surfaces likewise get newlines normalised (`normalize_paste_newlines`: single-line editors never want a raw CR); the edtui body path keeps CRLF so a pasted body is preserved. With the full curl now intact in the prompt buffer, the existing submit → `import_curl` path handles the `\`-continuations and `\[\]` glob-escapes. When no text surface is active (Normal mode with no inline editor, jump, a y/n confirm, a pending leader, the method menu) the paste is dropped — never replayed as keys.

**Import secret capture.** `import_curl` remaps `Authorization: Bearer <token>` / `-u user:pass` to first-class `Auth::Bearer`/`Auth::Basic` with the secret replaced by a `{{token}}`/`{{password}}` placeholder (no secrets in workspace files). `ImportResult.captured_secrets` now carries the extracted `(var_name, real_value)` pairs (empty when the value was already a placeholder or absent). The TUI import path (`create_endpoint_from_curl`) binds each into a RAM-only **Session var** via `write_session_var` so `{{token}}` resolves and the endpoint is immediately sendable; the file on disk still holds only the placeholder, and the real value is never echoed. An existing same-named session var is overwritten for now (dedup/rename deferred — see ROADMAP). The CLI (`run_import`) has no live session, so it leaves the placeholder and keeps the "supply via profile/env" warning; it never prints the captured value.

**CLI `churl import` input forms** (`Import.curl: Vec<String>`, `trailing_var_arg`): **0 tokens** (or a lone `-`) reads the whole command from **stdin** (`pbpaste | churl import`), guarded by an `IsTerminal` check that refuses with a usage hint rather than hanging on an interactive TTY; **1 token** is a full command STRING → `import_curl` (shlex-tokenises); **>1 tokens** are raw shell-tokenised args → `import_curl_tokens` parses them directly (no re-split), so `churl import curl 'url' -H '…'` works with `-H` etc. captured rather than parsed as churl flags. `--name`/`--out` must precede the command. Both string and token paths share one parser: `import_curl(s) = import_curl_tokens(shlex::split(normalize_continuations(s)))`.

Since M3 `AppMsg` also carries `Response { generation, outcome, meta }` (a stale generation — after cancel/resend — is dropped) and `Highlighted { hash, lines }`. The app channel is **bounded** (`mpsc::channel(APP_CHANNEL_CAPACITY=1024)`, R1 D4a) so a fast/high-`total` load run can't flood it with unbounded owned `Response`/`Highlighted` bodies; every sender is a spawned async task (`.send().await`) or the highlight OS thread (`blocking_send`), never the render/UI thread, so a full queue applies backpressure without stalling input. Render functions stay pure (`fn render(frame, area, &state)`); the response pane's `render` additionally *returns* an optional `HighlightJob` for the caller to enqueue (top-level `render` already takes `&mut App`). Highlight caches live per-`EndpointBuffer` (capped at 64) and are evicted from the previously-active buffer on every buffer switch (`set_active_buffer`, R1 D4c), bounding total cached lines to (active-buffers × 64). Under `TestBackend` no highlight worker exists, so snapshots deterministically show plain text.

**History-write reliability (R1.5 B).** On **quit**, the `run` loop exits and the tokio runtime drops, aborting any in-flight `send` task before its `Response` can land — so `record_inflight_on_quit()` runs at the end of `run` (runtime still alive) and writes an interrupted history row (`write_history(meta, None, None)`, mirroring `cancel_request`) for **every** buffer with an `in_flight` request, so a request completed-on-the-wire but quit before landing isn't silently lost (B1). A **persistent** history/load-summary write failure is surfaced stickily, not just as a 250 ms toast: `App.history_write_failures` counts consecutive `insert`/`insert_load_batch` failures (`note_history_write` resets to 0 on any success, called from both write sites), and once it reaches `HISTORY_FAIL_THRESHOLD` (=1) the statusline shows a persistent `⚠ history not recording` flag (`theme.status_error`) until a write succeeds (B3) — a healthy session never arms it, so all 95 snapshots are unaffected.

Key routing precedence (per key event, in `Mode::Normal` the crokey map is authoritative):

0. Jump-mode (`f` by default) is an overlay-level mode alongside search/palette: it consumes every key — a label char focuses that pane / selects that explorer row, `Esc` (or the Jump key again) cancels, everything else is ignored.
1. Open overlays consume everything: search `/` + palette `:` (`Esc` closes, `Enter` accepts, `Up`/`Down`/`Ctrl-n`/`Ctrl-p` move, chars edit the query); the method menu (`M`, a label key picks); CRUD prompts (`Mode::Prompt`, a `LineEditor` line for every purpose, where `Enter` commits unconditionally — `NewEndpoint` defaults here too, but escalates to the multi-line edtui vim editor (`CurlPrompt`) once a curl/multi-line paste or `Ctrl-e` expands it (`curl_prompt: Some(..)`), so a pasted browser curl is editable across lines: vim-faithful, Insert-mode `Enter` inserts a newline, Normal-mode `Enter` commits, `Esc` cancels in Normal mode); and confirms (`Mode::Confirm`; `y/n` for endpoint delete, `s`/`d`/`Esc` for discard-changes).
2. Inline editors in `Mode::Normal` intercept next: the URL bar editor (`url_editor`) and request-row field edits (`RequestTabs::editing`) each own the keyboard — except a CONTROL key the keymap resolves to `Send`/`Quit` (the M4 interception, generalised to `LineEditor`). `Enter` commits, `Esc` cancels.
3. Request pane, Body tab, edtui in a non-Normal mode: keys go to edtui with the same Ctrl-S/Ctrl-C interception (M4, DECISIONS.md).
4. Otherwise: `KeyMap::lookup_ctx(key, focused_pane.ctx())` — the focused pane's overlay wins over the global map (so `1`–`4` are tab jumps in the Request pane but pane-focus globally). Unmapped keys fall through to edtui only on the Body tab; navigation actions forward their key event to edtui there so vim motions keep working.

The Explorer pane overlay binds the arrow keys to navigation (`Up`/`Down` → Up/Down, `Left`/`Right` → Collapse/Expand), mirroring the global `k`/`j`/`h`/`l` (M7.10 follow-up). They are Explorer-scoped, not global, so Left/Right never leak Collapse/Expand into other panes; the flat sequences sub-pane no-ops Collapse/Expand, so they are harmless there.

The find/open pickers hang off the leader key as its own continuation (M7.10 follow-up): `<leader><leader>` (Space Space) opens the endpoint/request picker, `<leader>s <leader>` the sequence picker, `<leader>l <leader>` the load-runner endpoint picker. `f` is freed at root for jump-mode. Space as a leader *continuation* is not flagged by `validate` (which only checks the leader key against the global map + pane overlays).

The session **Options overlay** (M8, `components/options/`) is a keyboard-owning `Mode::Options(OptionsState)` variant (state owned by the variant, per the R1.5 A2 rule) opened by `<leader>o`: three control rows (Proxy — an inline `LineEditor`, userinfo masked in display and, M8.1, the password masked *within the userinfo while it is typed* — everything after the first `:` up to the first `@`/end, so a half-typed `user:pass` never renders in plaintext; the `host:port` after `@` is left visible; TLS verification on/off; Cookies on/off) plus a scrollable cookie list (`d` delete row · `x` clear all). It is UI-only — it emits an `OptionsOutcome` the app applies to session state (`session_proxy`/`session_insecure`/`cookies_enabled` + the `Arc<ChurlCookieJar>`), then rebuilds the single client via `rebuild_client`. `<leader>k` toggles insecure-TLS from anywhere; a loud RED "⚠ TLS OFF" flag rides the statusline whenever verification is off. Both are real keymap `Action`s (`options`/`toggle-insecure`), so they surface in `?` help + the palette.

The Response pane (M7) adds a `[keys.response]` overlay (`h` headers · `W` wrap · `p` pretty/raw · `s` sort JSON keys A→Z · `/` search · `n`/`N` match nav · `o`/`O` fold · `y`/`Y` copy · `H`/`L` (aliased Left/Right) horizontal pan for unwrapped long lines) and a new keyboard-owning overlay mode `Mode::BodySearch` (routed like the other overlays; the incremental `/query` input renders in the message-row position via the shared `LineEditor`). The response `[h]` headers-hint is focus-gated — it shows only when its response pane (main, sequence-runner, or load-runner) is focused.

### Response viewer pipeline (M7)

The response viewer composes a reformat pass plus three pure transforms over the logical lines (`components/response/`); the reformat/sanitize/index stages run once per response (on build / toggle), while the fold→wrap→viewport geometry is memoized per-frame by the R1.5 A3 geometry cache (below) so an idle tick over a multi-MB body no longer re-walks it:

```
raw body bytes → lossy-UTF-8 decode  (kept verbatim as `raw_text` — copy reads this)
  → reformat pass    (JSON-only, pretty-by-default; malformed → raw fallback)
  → sanitize pass    (strip ANSI · expand tabs @4 · CRLF→LF · controls→`·`) ⇒ displayed `text`
  → line index       (`line_offsets` over the DISPLAYED text — navigation matches what's shown)
  → fold filter      (JSON-only; folded regions elided to a `⋯ N lines` header)
  → wrap expansion    (optional; each display row = a char sub-range of one logical line;
                       unwrapped rows are windowed to `[h_scroll, h_scroll+width)`)
  → viewport slice    (scroll offset + height)
```

**Reformat pass (M7.7 Stage A).** `reformat_body_if_needed(text, syntax, pretty, sort_keys)` runs before the fold/wrap/viewport stages. When `pretty` is on and `syntax == Json` it parses the body as a `serde_json::Value` and re-emits it with `to_string_pretty` (JSON-only in v1 — zero new deps since `serde_json` is already a workspace dep); on **any** parse error it returns the raw text unchanged (silent fallback — the viewer never errors on a malformed body). json-ish content-types default to pretty on arrival, everything else to raw. `p` (`Action::TogglePretty`) flips raw↔pretty: it rebuilds `text`/`line_offsets` from `raw_text`, bumps `generation` (invalidating the syntect viewport cache), and **resets folds** (openers are position-based; the layouts differ). `pretty` is also hashed into `viewport_hash`. The separate `raw_text` field is what copy (`y`/`Y`) reads, so copy stays byte-exact regardless of the toggle — reformatting drives display only.

**Optional A→Z key sort (M7.7 Stage A follow-up).** A `sort_keys` flag (default **false**, per-response, nothing persisted) layers an optional alphabetical key sort onto the pretty view. `s` (`Action::ToggleSortKeys`) flips it via `ResponseView::toggle_sort_keys`, which mirrors `toggle_pretty` exactly (rebuild `text`/`line_offsets` through the reformat path, bump `generation`, reset folds, clear search); `sort_keys` is also hashed into `viewport_hash`. When set (and pretty-on + JSON), `sort_value_keys` recursively rebuilds every `serde_json::Value::Object`'s entries in `String::cmp` order before `to_string_pretty` — **arrays keep element order**, only object keys sort. It works because the workspace's `preserve_order` feature makes the rebuilt map re-emit in the insertion (= sorted) order written. Sort is **independent of `pretty`**: it only affects display while pretty is on; the app handler guards on pretty-JSON-body and otherwise no-ops with a `sort: pretty JSON only` notice. Default stays wire order; copy is unaffected (still `raw_text`, byte-exact).

**Sanitize pass + horizontal window (M7.7 close-out).** `sanitize_for_display(text)` runs on the reformatted text *before* `index_lines` (never on `raw_text`, so copy stays byte-exact): a single left-to-right scan strips ANSI escapes (CSI/OSC/lone-ESC — zero-width; blocks cursor moves, recolours, OSC-52 clipboard writes), expands `\t` to the next multiple of `TAB_WIDTH = 4` (a documented const, not config; columns reset at each `\n`), collapses `\r\n`→`\n` (a lone `\r` is a control char, so no phantom blank lines), and replaces every other C0/DEL/C1 control with `·` (U+00B7). Correct JSON is already clean; the teeth are on the raw / parse-failed / `text/plain` path. Separately, an `h_scroll` char-column offset on `ResponseView` bounds unwrapped long-line render cost: `expand_wrap` windows each unwrapped **normal-line** row to `[h_scroll, h_scroll + width)` (fold-headers keep full width; wrap unchanged) — row *count* is unchanged, so the display-row↔logical mapping stays intact. `H`/`L` (aliased to Left/Right; `Action::ScrollBodyLeft`/`Right`, remappable) pan it wrap-off-only; `render_done` clamps it to the widest visible line (mirror of `clamp_scroll`) and reports `clamped_h_scroll` for write-back; search-into-view pans a far-right match into the window (`ensure_column_visible`). `h_scroll` resets on build/generation-bump/`ToggleWrap` and keys `viewport_hash`.

Cursor and scroll are **display-row** indices (post-fold, post-wrap); the cursor follows-and-scrolls at render time. Search matches are stored against *logical* lines (byte ranges) and mapped through the pipeline for navigation, auto-unfold, and highlight overlay. Per-view UI state (view mode, folds, wrap, pretty, sort-keys, search, `h_scroll`) lives on `ResponseView` so it resets on each new response; cursor/scroll/viewport geometry live in a `response::ResponseGeometry { scroll, cursor, total_rows, viewport_height, viewport_width }`. Fold regions come from `components::fold::scan_regions` (single O(n) string-aware pass over the displayed text, cached lazily). Highlighting is deferred under wrap (wrapped bodies render plain); unwrapped bodies keep full off-thread syntect highlighting with a duplicate-enqueue guard (`App::pending_highlight`).

**Render geometry cache (R1.5 A3).** The fold→wrap→viewport geometry — the fold-filtered `Vec<Visible>` (O(logical lines)), the wrap-expanded `Vec<DisplayRow>` (O(total body chars)), and `max_h_scroll`'s widest-visible-line scan (O(total visible chars)) — was recomputed **every frame, including idle 250 ms ticks**, so a multi-MB body cost O(body) per tick even when nothing changed. It is now memoized in a single-slot `GeomCache` (a `RefCell` field on `ResponseView`, so interior-mutable through the pure `&self` render + geometry accessors) with one slot per product, each keyed on the signature that captures exactly its inputs: the display-rows slot on `RowsSig { visible_sig, wrap, width, h_scroll }`, the widest-line slot on `MaxHScrollSig { visible_sig, wrap, width }` (deliberately **omitting** `h_scroll` — the widest line is pan-independent, so a pure horizontal scroll still hits it), where `visible_sig = { generation, headers, fold_sig }` covers every displayed-text rebuild (`generation` — pretty/sort bump it), the body↔headers `view_mode` switch (`headers`), and the folded-opener set (`fold_sig`, the same sorted-openers hash the highlight `viewport_hash` uses). A signature match ⇒ a recompute would be byte-identical, so serving the memo is behaviour-preserving (the 95 `tui_snapshot` snapshots prove it); a miss recomputes and **replaces** the slot (never grows), and the whole cache dies with the view when a new response replaces it — matching the per-`EndpointBuffer` highlight cache's bound + generation discipline. `line_numbers` is not a separate key input: it feeds the pipeline only by shrinking the effective `width` (gutter) before the geometry calls, so keying `width` covers it. **Deliberately deferred:** the audit's cheaper alternative — a dirty-flag gate on `terminal.draw` — is **not** shipped, because gating the whole draw risks a stale UI if any tick-driven update (in-flight spinner, elapsed-`ms` readout, message expiry) is missed; the geometry cache removes the per-frame O(body) recompute while every tick still redraws, keeping animations live.

**Unified runner response viewer (drive-test note #2).** `ResponseGeometry` is carried by the main-pane endpoint buffer AND by both runner states (`LoadRunnerState`/`SequenceRunnerState`). `App::active_response_surface()` resolves `Main | LoadRunner | Sequence` from `self.mode` + the runner's `focus`; `active_response()` / `active_response_geometry()` (+ `_mut`) route through it, so the `response_*` handlers are mode-aware without per-handler branching. A runner's Response-region key is dispatched through the SAME `lookup_ctx(_, PaneCtx::Response)` + `response_*` handlers the main pane uses (`App::try_route_runner_response_key`, called before delegating to the runner) — full parity (headers/wrap/pretty/sort/line-numbers/fold/search/copy/hscroll + cursor nav), zero duplication. The two runners no longer carry their own `handle_response_key`. The highlight cache/guard stays on the active endpoint buffer (shared by all three surfaces). Body search opened over a runner restores `App::body_search_return` on close so it returns to the runner, not to Normal.

**Honest, yankable failed row (drive-test #4a).** A transport failure is `ResponseState::Failed { error, meta }` (never `Done`), so it carries no `ResponseView`. Because the copy handler `response_copy_view` reads through `response_view_mut()` (which is `Some` only for `Done`), `y` was a silent no-op on a failed row. The fix stays in the shared viewer: the `response::render` `Failed` arm renders an honest panel — the request method+URL (from `meta`), the error, and an explicit "no response — status, body, timing unavailable" line (a transport failure genuinely has none; nothing is fabricated) — and `ResponseState::failure_copy_text() -> Option<String>` yields the copyable blurb (request line + error) for `Failed`, `None` otherwise. `response_copy_view` gains one state-branch: copy the view when `Done` (unchanged byte-exact path), else `enqueue_clipboard(failure_copy_text())`. Both live in the single shared surface, so the main pane and both runners get it without per-site duplication. `Dropped` (memory-evicted load rows) has no body bytes and returns `None` — `y` stays a deliberate no-op there.

**Line-number gutter (drive-test note #8).** A `line_numbers: bool` on `ResponseView` (default **true**, toggled by `#` / `Action::ToggleLineNumbers`) draws a dim, right-aligned left gutter showing each display row's **1-based displayed logical line number**. The number shows on the first display row of each visible entry (tracked by `visible_idx`, robust to `h_scroll`); wrapped-continuation rows show a blank aligned gutter; fold-headers show the opener line's number. `ResponseView::gutter_width()` = digits of the **total displayed line count** + 1 pad (stable while scrolling; `0` when off). **Width seam**: `render_done` subtracts `gutter_width` from `body_area.width` **once** and threads the reduced width through `max_h_scroll`, `expand_wrap` (wrap split + `h_scroll` window), and `RenderOutcome::viewport_width` — the caller feeds `viewport_width` back into `logical_at_display_row` / `display_row_for_logical` / `ensure_column_visible`, so cursor↔logical mapping and search-into-view use the same gutter-adjusted width. Render-only: `raw_text`, copy, and `line_offsets` are untouched (`viewport_hash` keys on `line_numbers` so a toggle re-highlights at the new width). The gutter also appears in the load-runner / sequence-runner responses via the shared `ResponseView`.

**Shared smart-case matcher (M7.7 Stage B).** The body-search matching algorithm lives in one place — `response::smart_case_matches(lines, query) -> Vec<(line, start, end)>` (smart-case: all-lowercase = case-insensitive, any uppercase = case-sensitive; unicode case-fold offsets via `lowercase_with_offsets`/`map_lowered_offset`). `ResponseView::compute_matches` delegates to it, and the `?` help overlay's `/` search calls it over the help lines' plain text, so the two searches can never diverge. The match-span painter `build_search_spans` (current match reversed, others dim+underlined) is shared too. **Help-overlay search** (`Mode`-free — the help overlay intercepts keys ahead of the mode machine) is **highlight-and-jump, never a hiding filter**: state is `help_search: Option<help::HelpSearch>` + `help_search_editor: LineEditor` + `help_search_input: bool` on `App`; `/` opens it inside `handle_help_key`, `handle_help_search_key` mirrors `handle_body_search_key` (typing recomputes + jumps, `n`/`N` cycle/wrap, Esc clears keeping the overlay open, Enter commits keeping matches), and `help::render` paints highlights + a `k/N` counter in the title while leaving every row present.

## On-disk format

```
<workspace>/                    # a git repo the user owns == the ROOT COLLECTION (M7.9)
  churl.toml                    # root-collection meta: name + [vars] + [[profiles]] (no secrets)
  <endpoint>.toml               # root-level endpoints attach directly to the root collection
  <collection>/                 # a directory = a collection; nests to arbitrary depth (M7.9)
    folder.toml?                # optional sub-collection metadata + flat [vars] defaults (M6)
    <endpoint>.toml             # one file per endpoint; explicit `seq` for ordering
    <sub-collection>/           # collections nest recursively — no depth cap
      folder.toml?
      <endpoint>.toml
  sequences/                    # reserved dir (M7.4) at the ROOT ONLY — request sequences,
    <sequence>.toml             #   NOT a collection; a nested `sequences/` IS a collection
```

M7.9 folded the workspace into the **root collection**: everything is a collection, the workspace root *is* the root collection, collections nest to arbitrary depth, and endpoints attach to any node (root included). `churl.toml` is the root collection's meta (name + `[vars]` + `[[profiles]]`); sub-collections carry only `folder.toml` (vars). The root additionally owns the global `sequences/` store and profiles. Var resolution walks the endpoint's collection ancestor chain leaf→root (inherit-and-override) — see the `template` module and DECISIONS.md "Variable scoping". A pre-M7.9 flat workspace loads unchanged (its `churl.toml` `[vars]` already are the root vars; root endpoints are net-new files) — no migration.

Sequence file shape (M7.4):

```toml
seq = 0
name = "Auth flow"
on_error = "halt"               # halt (default) | continue

[[step]]
seq = 0
endpoint = "auth/login.toml"    # workspace-relative endpoint path
persist = ["token"]             # note #6: these rules' captured values feed the
                                #   in-memory Session scope (surviving the run);
                                #   omitted/empty => every rule is Run-only (ephemeral)
[step.extract]
token = "$.data.token"          # var name -> extraction expression
user_id = "$.data.user.id"

[[step]]
seq = 1
endpoint = "users/me.toml"      # its request uses {{token}} — resolved from the extracted scope
```

**In-memory Session store (note #6).** `persist` lists only the rule *names*; the captured *values* are written into a process-lifetime, in-memory Session store (`App::session_vars`, keyed by canonical workspace root) and are **NEVER written to disk** — a security feature, so captured secrets never touch the filesystem and evaporate on exit. The Session scope is the highest-precedence resolver layer (`session > cli > …`) for both sequence-run and standalone-send resolution, so a token captured by a `login` sequence resolves in a later standalone `{{token}}` send. The `<leader>v` env editor shows the store as a read-only, masked `Session` group with a clear action.

Endpoint file shape (M1):

```toml
seq = 1                     # explicit ordering within the collection
name = "Get user"

[request]
method = "GET"
url = "https://api.example.com/users/{{id}}"

[[request.headers]]         # array-of-tables; `enabled = false` to disable a line
name = "Accept"
value = "application/json"

[request.body]              # optional; type = text|json|form (default text)
type = "json"
content = '{"q": true}'

[request.auth]              # optional (M5); type = basic|bearer|apikey
type = "basic"              # secret values must be {{var}} placeholders —
username = "alice"          # save_endpoint/endpoint_to_toml refuse literals
password = "{{password}}"

[[assertions]]               # optional (M8.4); top-level, sibling of [request] —
target = "status"            # a property of the saved invocation, like a
op = "=="                    # sequence step's [step.extract], not of the
value = "200"                # wire-shaping request itself
[[assertions]]
target = "$.data.id"          # `value` omitted on disk for exists/absent
op = "exists"
```

Saves are format-preserving: comments and ordering in hand-edited files survive a churl round-trip (see DECISIONS.md for merge semantics and edge cases). Every save is also **atomic and durable** (R0): the single write in `save_value` funnels through `persistence::atomic_write` (temp sibling file → fsync → atomic `rename` → parent-dir fsync), so a crash mid-write can never tear the source-of-truth file. The error contract is unchanged (`PersistenceError::Write`).

Workspace `churl.toml` and collection `folder.toml` (M6):

```toml
# churl.toml (the root collection's meta, M7.9)
name = "my-api"

[vars]                        # root-collection template defaults, outermost scope (no secrets)
base_url = "https://api.example.com"

[[profiles]]                  # per-environment; profile vars beat collection vars
name = "prod"
[profiles.vars]
host = "prod.example.com"

# <collection>/folder.toml
[vars]                        # collection-level defaults, environment-independent
page_size = "50"
```

Global config `~/.config/churl/config.toml` (M6 adds theming):

```toml
theme = "dark"                # built-in: "dark" (default) | "light"

[theme_colors]                # per-slot overrides (named ANSI or #rrggbb)
title = "cyan"                # unknown slot / bad colour = loud startup error
jump_label = "#ffcc00"        # (table is [theme_colors], not [theme.colors] — see DECISIONS.md)

[keys]                        # action remaps: "combination" = "action-name"
"ctrl-p" = "open-palette"
```

High-churn state (history, cookies, cached responses) lives in:
```
$XDG_DATA_HOME/churl/state.sqlite
```
Never in the workspace — workspace = safe to commit/sync; state.sqlite = never committed.

Render-side caches (line-offset index, wrap layout, viewport-only syntect highlighting) are in-process only — not persisted.

## Performance budget

| Concern | Target | Mechanism |
|---|---|---|
| Cold start | < 100 ms | Lazy collection loading; lazy syntect init |
| 1 MB response body | Smooth scroll | Virtualised line render; line-offset index; viewport-only highlighting |
| Syntax highlighting | < 23 ms per viewport | `syntect` + `two-face` colours, off-thread, cached by viewport hash |

No HTTP-semantic caching by design — churl is a development tool; stale responses are a footgun. History covers recall.

## Init scaffold (`churl init`)

`crates/churl/src/init.rs` — the `Init` subcommand scaffolds a workspace in the cwd (or `[path]`) so a first-time user can send a request in under a minute. Plain `init` writes only a blank root manifest; `init --demo` additionally scaffolds the three-endpoint demo collection the removed `churl tutorial` subcommand used to (M8.2 hard-removed `tutorial` — no alias; see DECISIONS.md). Refuses only when a `churl.toml` already exists at the target — unlike the old `tutorial`, it does not require the whole directory to be empty (running `init` inside an existing project directory, alongside unrelated files, is the normal case).

The scaffold writes through the real `churl-core::persistence` seams — no hand-written TOML strings for endpoint or folder files:

1. `save_workspace_manifest(root, &ws)` — writes `churl.toml` (blank: just `name`; `--demo`: adds `vars` (`base_url`) and a `dev` profile).
2. `--demo` only: `create_collection(root, "examples")` — creates the collection directory.
3. `--demo` only: `save_collection_meta(coll_dir, &Default::default())` — writes `examples/folder.toml` (empty vars).
4. `--demo` only: three `create_endpoint(coll_dir, name)` + `save_endpoint(path, &ep)` calls — writes the endpoints through the format-preserving merge serializer.

`--demo` endpoints target [httpbingo.org](https://httpbingo.org): Get Anything (GET `/anything?name=churl`), Post JSON (POST `/post` with JSON body), Bearer Auth (GET `/bearer` with `{{token}}` placeholder auth).

## CLI & headless (M8.2)

`crates/churl/src/{output,headless,resolve,run_cmd,send_cmd,seq_cmd,load_cmd}.rs` — the headless execution surface: `churl run <endpoint>`, `churl send`, `churl run-seq <name>`, and `churl load <endpoint>` drive the exact same `churl_core::http::execute`/`Resolver`/persistence (and, for `run-seq`, `churl_core::sequence`; for `load`, `churl_core::load`) seams the TUI uses (no forked request logic), then shape the result into the frozen `--json` envelope (single object for `run`/`send`/`load`, NDJSON stream for `run-seq`). Full contract (schema, error-kind → exit-code mapping, per-command payload shape, the sequence NDJSON stream, the load aggregate block): [`docs/CLI.md`](CLI.md).

- `output.rs` — the `Envelope`/`ErrorKind`/`CliError` machinery every headless command funnels through via `output::emit`, so stdout/stderr/exit-code stay consistent across `run`/`send`/`import`. `output::emit`'s generic bound also carries the M8.4 `SuccessExitCode` trait — the seam that lets a successful `run`/`send` still exit 1 when its assertions failed, without touching `ErrorKind`. `output::emit_error` (factored out of `emit`) prints the single failure envelope; `run-seq` reuses it for its pre-flight errors. `ErrorKind::SequenceNotFound` (band 3, M8.4.1) is additive.
- `headless.rs` — `run_execution`: resolver substitution → unresolved-`{{var}}` refusal → `build_client_with` → `execute` → then delegates envelope-payload shaping to `shape_exec_data(request, response, assertions, trace)` — a pure fn (assertion evaluation via `churl_core::assert::run_assertions`, secret-masked request echo, utf8/base64 body) that is the **single source of truth for the per-request `data` shape**, called by both the single-request path here and each step of `seq_cmd` so their `data` is byte-identical.
- `seq_cmd.rs` (M8.4.1) — `churl run-seq <name>`: the headless request-sequence runner. Resolves `sequences/<name>.toml` by file stem (enumerating real files — traversal-safe), then loops the core engine seams (`ordered_steps` → `prepare_step` → `execute_traced` → `classify_step` → `should_halt`), gating each step on its endpoint's persisted `[[assertions]]` (carried through `PreparedStep::assertions`) via `shape_exec_data`, and chaining extracted values in-process. Owns its own stdout/exit (a stream, not one envelope): emits one NDJSON step line as each step completes plus a terminal summary line; exit = first hard step error's band (3/4/5), else 1 on any failed assertion or broken extraction chain, else 0.
- `resolve.rs` — `collection/.../endpoint name` path resolution against `churl_core::persistence` directly (no live `ExplorerState` tree in a one-shot process), plus `--profile` validation shared with `send`/`run-seq`.
- `run_cmd.rs` / `send_cmd.rs` — CLI-argument-to-execution glue: `run` resolves a saved endpoint and its collection var-scope chain, then merges its persisted `[[assertions]]` with any `--assert` flags (M8.4, append); `send` builds an ad-hoc `Request` from curl-mnemonic/churl-native flags (`-X`/`-H`/`-d`/`--url` vs `--method`/`--header`/`--body`), no saved endpoint or workspace required, and its `--assert` flags are the whole assertion set.
- `load_cmd.rs` (M8.4.2) — `churl load <endpoint>`: the headless load runner. Resolves the endpoint + collection var chain exactly like `run`, substitutes/refuses `{{var}}` once, builds one client, and fires `churl_core::load::run_load` (the wiremock-tested engine the TUI shares), measuring wall-clock around the call for `rps` (the core signature stays un-widened). Emits **one** aggregate envelope via `output::emit` (not a stream — a load run is one result): `data.{config,stats,assertions}`. Aggregate assertions use the `stats.<field>` namespace: `--assert` strings are parsed by `churl_core::assert::parse_stats_assertion` (reusing the response `<target> <op> <value>` grammar verbatim, only the target resolver differs) and evaluated by `run_stats_assertions` against the run's `LoadStats` + `rps`. A failed `stats.*` assertion → exit 1 (via `SuccessExitCode`), success-shaped, exactly like `run`.

## Release pipeline

Tag-triggered GitHub Actions workflow (`.github/workflows/release.yml`):
- `taiki-e/upload-rust-binary-action@v1` — cross-compiles, strips, archives, and uploads per-target binaries with SHA-256 checksums.
- Targets: `aarch64-apple-darwin`, `x86_64-apple-darwin`, `x86_64-unknown-linux-musl` (static), `aarch64-unknown-linux-musl` (static), `x86_64-pc-windows-msvc`.
- musl static targets are viable because the HTTP stack is rustls (pure Rust TLS) and SQLite is bundled — no system-lib dep.

`install.sh` (repo root, POSIX sh) — the `curl|sh` installer: detects OS/arch → release target triple, downloads the `.tar.gz` + `.sha256` from GitHub Releases, verifies checksum, extracts to `~/.local/bin`. Options: `--to DIR`, `--force`, `--dry-run`.
